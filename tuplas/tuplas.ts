let crew: String[] // arr de strings
let adress: [string, string, number] // arr de apenas 2 strings

adress = ['rua A', 'bairro tal', 800]

let [x, y, z] = adress //retornando tuplas
//let x = crewOfTwo[0]
//let y = crewOfTwo[1]